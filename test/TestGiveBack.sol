pragma solidity ^0.4.24;
import "truffle/Assert.sol";
import "truffle/DeployedAddresses.sol";
import "../contracts/Charity.sol";
import "../contracts/Contributions.sol";
import "../contracts/Donors.sol";

contract TestGiveBack{
  /*function testCreateMission() {
    //Charity charity = Charity(DeployedAddresses.Charity());
    //charity.createMission('first', 10, tx.origin);
    uint expected = 1;
    //Assert.equal(1,1,"");
    Assert.equal(uint(1), uint(1), "correct");
    //Assert.equal(charity.missionCounter), expected, "Charity should have one mission");
  }
  function testTEST() public{
    Assert.equal(uint(1), uint(1), "correct");
  }*/

}
